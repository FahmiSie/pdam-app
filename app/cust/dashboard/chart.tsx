// "use client";

// import { Bills } from "@/types/bills";

// export function BillChart({data}: {data: Bills[]}){

//  const chartData = data.map(bill) => ({
//     monthL new Date(bill, year, bill.month -1).toLocaleString("id-ID", {

//     })
//  }) }
